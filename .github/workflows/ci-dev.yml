---
name: CI - Dev (Build + PR to Hom)

on:
  push:
    branches:
      - dev
    paths-ignore:
      - '**.md'

jobs:
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build JAR
        run: mvn clean package -DskipTests

      - name: Build Docker Image
        run: |
          docker build \
            -t process-video:${{ github.sha }} \
            .
