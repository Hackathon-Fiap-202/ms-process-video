apiVersion: v1
kind: Service
metadata:
  name: process-video
  namespace: default
  labels:
    app: process-video
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: process-video
